<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Flutter needs a local VM service connection for debugging and hot reload. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
